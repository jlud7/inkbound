import React from 'react';
import ReactDOM from 'react-dom/client';
import Inkbound from './Inkbound.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Inkbound />
  </React.StrictMode>
);
